<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <!--
      Clickable so a tap that misses one of the buttons on it is swallowed here rather than falling
      through to the video, where it would play or pause. The buttons are drawn over it and still
      take their own taps. Ignored below API 16, where the worst of it is a stray TalkBack target.
    -->
    <View
        android:id="@+id/overlayTopBackground"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/black_alpha"
        android:clickable="true"
        android:importantForAccessibility="no"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/overlayBackButton"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:contentDescription="@string/action_close"
        android:focusable="true"
        android:padding="6dp"
        android:scaleType="centerInside"
        app:layout_constraintBottom_toBottomOf="@id/overlayTopBackground"
        app:layout_constraintStart_toStartOf="@id/overlayTopBackground"
        app:layout_constraintTop_toTopOf="@id/overlayTopBackground"
        app:srcCompat="@drawable/ic_back_24dp"
        app:tint="@color/palette_white"
        tools:ignore="MissingPrefix" />

    <TextView
        android:id="@+id/overlayCounterText"
        style="@style/Widget.Vector.TextView.Body"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/palette_white"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/overlayInfoText"
        app:layout_constraintEnd_toStartOf="@id/overlayDownloadButton"
        app:layout_constraintStart_toEndOf="@id/overlayBackButton"
        app:layout_constraintTop_toTopOf="@id/overlayTopBackground"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="1 of 200" />

    <TextView
        android:id="@+id/overlayInfoText"
        style="@style/Widget.Vector.TextView.Body"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/palette_white"
        app:layout_constraintBottom_toBottomOf="@id/overlayTopBackground"
        app:layout_constraintEnd_toStartOf="@id/overlayDownloadButton"
        app:layout_constraintStart_toStartOf="@id/overlayCounterText"
        app:layout_constraintTop_toBottomOf="@id/overlayCounterText"
        tools:text="Bill 29 Jun at 19:42" />

    <ImageView
        android:id="@+id/overlayMoreButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:contentDescription="@string/attachment_viewer_more_options"
        android:focusable="true"
        android:padding="6dp"
        app:layout_constraintBottom_toBottomOf="@id/overlayTopBackground"
        app:layout_constraintEnd_toEndOf="@id/overlayTopBackground"
        app:layout_constraintTop_toTopOf="@id/overlayTopBackground"
        app:srcCompat="@drawable/ic_more_horizontal"
        app:tint="@color/palette_white"
        tools:ignore="MissingPrefix" />

    <ImageView
        android:id="@+id/overlayDownloadButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:contentDescription="@string/action_download"
        android:focusable="true"
        android:padding="6dp"
        app:layout_constraintBottom_toBottomOf="@id/overlayTopBackground"
        app:layout_constraintEnd_toStartOf="@id/overlayMoreButton"
        app:layout_constraintTop_toTopOf="@id/overlayTopBackground"
        app:srcCompat="@drawable/ic_material_save"
        app:tint="@color/palette_white"
        tools:ignore="MissingPrefix" />

    <androidx.constraintlayout.widget.Group
        android:id="@+id/overlayVideoControlsGroup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:constraint_referenced_ids="overlayBottomBackground,overlayBackButton,overlaySeekBar,overlayVideoTime"
        tools:visibility="visible" />

    <!-- Clickable for the same reason as the top bar: it holds the scrubber. -->
    <View
        android:id="@+id/overlayBottomBackground"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="@color/black_alpha"
        android:clickable="true"
        android:importantForAccessibility="no"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Play/pause lives over the video itself; only when the chrome has been hidden and brought
         back once, the way Telegram's viewer does it. -->
    <ImageView
        android:id="@+id/overlayCenterPlayPause"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:background="@drawable/bg_video_center_play_pause"
        android:clickable="true"
        android:contentDescription="@string/action_play"
        android:focusable="true"
        android:padding="18dp"
        android:scaleType="fitCenter"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_play_arrow"
        app:tint="@color/palette_white"
        tools:ignore="MissingPrefix"
        tools:visibility="visible" />

    <!-- Scrub preview frame; sized to the video's aspect within a 150dp box when frames arrive,
         and slid along above the thumb. -->
    <ImageView
        android:id="@+id/overlaySeekPreview"
        android:layout_width="150dp"
        android:layout_height="84dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/bg_seek_preview"
        android:importantForAccessibility="no"
        android:scaleType="centerCrop"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@id/overlayBottomBackground"
        app:layout_constraintStart_toStartOf="parent"
        tools:visibility="visible" />

    <!-- Taller than the widget's natural height purely for a larger grab zone; the asymmetric
         vertical padding keeps the track where it was and puts all the extra region ABOVE it,
         so grabbing never reaches down toward the navigation area. -->
    <SeekBar
        android:id="@+id/overlaySeekBar"
        android:layout_width="0dp"
        android:layout_height="44dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:paddingTop="16dp"
        android:paddingBottom="0dp"
        android:backgroundTint="@color/palette_white"
        android:progressBackgroundTint="@color/palette_white"
        android:thumbTint="@color/palette_white"
        app:layout_constraintEnd_toStartOf="@id/overlayVideoTime"
        app:layout_constraintStart_toStartOf="@id/overlayBottomBackground"
        app:layout_constraintTop_toTopOf="@id/overlayBottomBackground" />

    <TextView
        android:id="@+id/overlayVideoTime"
        style="@style/Widget.Vector.TextView.Caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:layout_marginRight="16dp"
        android:textColor="@color/palette_white"
        app:layout_constraintBottom_toBottomOf="@id/overlayBottomBackground"
        app:layout_constraintEnd_toEndOf="@id/overlayBottomBackground"
        app:layout_constraintTop_toTopOf="@id/overlayBottomBackground"
        tools:text="1:12 / 3:40" />
</merge>
