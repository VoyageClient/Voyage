<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <!-- fillViewport stretches the wrapper to the viewport so center_vertical centres the block when it
         fits; when a tall invite (large display name, big font, short/landscape screen) overflows, the
         wrapper grows past the viewport and the ScrollView scrolls. The ScrollView height is fixed to the
         parent in LARGE mode (so it can scroll internally) and switched to wrap_content for the SMALL bar
         in VectorInviteView.render(). -->
    <ScrollView
        android:id="@+id/inviteScroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:scrollbars="none"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <!-- The small bottom margin nudges the centred block slightly above the exact middle (which otherwise
         reads as too low because the toolbar sits above this view); kept modest so it doesn't leave a
         band-like gap below. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/inviteContent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <ImageView
            android:id="@+id/inviteAvatarView"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_marginStart="8dp"
            android:layout_marginLeft="8dp"
            android:layout_marginTop="24dp"
            android:layout_marginEnd="8dp"
            android:layout_marginRight="8dp"
            android:importantForAccessibility="no"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@sample/user_round_avatars" />

        <TextView
            android:id="@+id/inviteNameView"
            style="@style/Widget.Vector.TextView.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:paddingStart="@dimen/layout_horizontal_margin"
            android:paddingLeft="@dimen/layout_horizontal_margin"
            android:paddingEnd="@dimen/layout_horizontal_margin"
            android:paddingRight="@dimen/layout_horizontal_margin"
            android:textColor="?vctr_content_primary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inviteAvatarView"
            tools:text="@sample/users.json/data/displayName" />

        <TextView
            android:id="@+id/inviteIdentifierView"
            style="@style/Widget.Vector.TextView.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:paddingStart="@dimen/layout_horizontal_margin"
            android:paddingLeft="@dimen/layout_horizontal_margin"
            android:paddingEnd="@dimen/layout_horizontal_margin"
            android:paddingRight="@dimen/layout_horizontal_margin"
            android:textColor="?vctr_content_secondary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inviteNameView"
            tools:text="@sample/users.json/data/id" />

        <TextView
            android:id="@+id/inviteLabelView"
            style="@style/Widget.Vector.TextView.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/send_you_invite"
            android:textColor="?vctr_content_secondary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inviteIdentifierView" />

        <im.vector.app.core.platform.ButtonStateView
            android:id="@+id/inviteRejectView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="2dp"
            android:layout_marginRight="2dp"
            android:minWidth="120dp"
            app:bsv_button_text="@string/action_reject"
            app:bsv_loaded_image_src="@drawable/ic_tick"
            app:bsv_use_flat_button="true"
            app:layout_constraintHorizontal_chainStyle="packed"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/inviteAcceptView"
            app:layout_constraintTop_toTopOf="@id/inviteAcceptView"
            app:layout_constraintBottom_toBottomOf="@id/inviteAcceptView" />

        <im.vector.app.core.platform.ButtonStateView
            android:id="@+id/inviteAcceptView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginStart="2dp"
            android:layout_marginLeft="2dp"
            android:minWidth="120dp"
            app:bsv_button_text="@string/action_accept"
            app:bsv_loaded_image_src="@drawable/ic_tick"
            app:bsv_use_flat_button="false"
            app:layout_constraintStart_toEndOf="@id/inviteRejectView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inviteLabelView" />

        <Button
            android:id="@+id/inviteIgnoreView"
            style="@style/Widget.Vector.Button.Text.Destructive"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="24dp"
            android:text="@string/action_ignore"
            app:layout_constraintEnd_toEndOf="@id/inviteAcceptView"
            app:layout_constraintStart_toStartOf="@id/inviteRejectView"
            app:layout_constraintTop_toBottomOf="@id/inviteAcceptView" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    </FrameLayout>

    </ScrollView>

</merge>
