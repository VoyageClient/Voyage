<?xml version="1.0" encoding="utf-8"?>
<!-- Extracted from the msg_bubble_*_incoming layer-lists: inline <vector> tags can't be inflated
     inside a layer-list pre-21, but a referenced vector loads fine via AppCompat. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="@dimen/sc_bubble_tail_size"
    android:height="@dimen/sc_bubble_tail_height"
    android:viewportWidth="10.0"
    android:viewportHeight="10.0">

    <path
        android:pathData="M0,0 L10,10 L10,0 Z"
        android:fillColor="?sc_message_bg_incoming" />

</vector>
