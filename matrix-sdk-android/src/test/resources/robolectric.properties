# The module targets SDK 35 but Robolectric 4.13 supports up to 34. Pin the test runtime so the
# database (and other Robolectric) tests run on a supported SDK. Low-API behaviour is validated
# on-device; these tests validate SQL/driver correctness, which is API-independent here.
sdk=34
